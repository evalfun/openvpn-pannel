# OpenVPN 面板 使用手册

> 一个自托管的 OpenVPN 管理面板：统一管理**用户**、**用户组**、**OpenVPN 服务器实例**，并通过
> `iptables`/`ipset` 做访问控制、通过 `tc` 做带宽限速。面板负责“编排”，真正的连接与转发由 OpenVPN 完成。


## 目录

1. 核心概念
2. 快速开始
3. 命令行（CLI）
4. 配置文件 config.json
5. 面板功能
6. 权限模型
7. ACL 与防火墙
8. 带宽限速（tc）
9. 资源与脚本
10. 证书与客户端配置
11. 日志、状态与轮换
12. 安全建议
13. 部署（systemd）
14. 常见问题（FAQ）
15. 附录

---

## 1. 核心概念

| 概念 | 说明 |
| --- | --- |
| 面板（Web） | 用户/用户组/服务器/资源/日志的可视化管理界面，仅 `admin` 组可登录。 |
| 内部 API | `internal_api_listen`，只监听本机；认证脚本、上下线脚本通过它回调查询权限、ACL、限速。 |
| 服务器实例 | 一个 OpenVPN 进程 + 一套工作目录（证书、`server.conf`、日志、脚本）。 |
| 工作目录 | `working_dir/<服务器ID>/`，每个实例独立。 |
| 用户组权限 / ACL | 决定客户端登录哪台服务器、能访问哪些网段。 |
| 限速策略 | 决定客户端上/下行速率，客户端上线时由 `tc` 自动生效。 |
| 事件 | 上线/下线/认证成功或失败/服务器启停/ACL 增删等审计记录。 |

### 方向约定（重要）

- **上传**：服务器 → 客户端（面板/服务端视角的“发送”，`tc` 出方向）。
- **下载**：客户端 → 服务器（面板/服务端视角的“接收”，`tc` 入方向）。
- 流量统计中的 `↑` 为上传、`↓` 为下载，含义与上面一致。

> 下载（`tc` 入方向）无法直接整形：优先把流量 `redirect` 到 `ifb<服务器ID>` 设备再用 HTB 整形
> （依赖内核 `ifb` + `act_mirred`，OpenWrt 上即 `kmod-ifb` + `kmod-sched-act-mirred`）；
> 若不可用则回退到 ingress `police`（依赖 `act_police`）；两者都缺失时会记录 WARNING 并跳过下载限速，
> 上传限速不受影响。

---

## 2. 快速开始

```bash
# 1) 生成一份示例配置
./openvpn-pannel democonfig > config.json

# 2) 修改config.json配置文件

# 3) 初始化数据库（首次部署、或升级版本后执行）
./openvpn-pannel -config=config.json migratedb

# 4) 创建管理员用户：  用户名  密码  描述
./openvpn-pannel -config=config.json createuser admin 'ADMIN_PASSWORD_111222333' 管理员

# 5) 创建 admin 用户组（名称必须为 admin）
./openvpn-pannel -config=config.json creategroup admin

# 6) 把管理员加入 admin 组
./openvpn-pannel -config=config.json addusertogroup admin admin

# 7) 启动面板
./openvpn-pannel -config=config.json run
```

启动后浏览器访问 `http://<主机>:8081`，用 `admin` 登录。

> - `admin` 组是特殊组：**只有 admin 组成员能登录面板**，普通用户只能用于 VPN 认证。
> - 建议不要把 VPN 用户放进 `admin` 组。
> - 修改服务器配置后需要**重启**该服务器实例才生效；修改客户端配置（CCD）后需**客户端重连**才生效。

---

## 3. 命令行（CLI）

用法：`./openvpn-pannel [-config=<路径>] <命令>`。`-config` 默认值为 `config.json`。

| 命令 | 说明 | 示例 |
| --- | --- | --- |
| `migratedb` | 迁移/初始化数据库（建表、加字段） | `./openvpn-pannel -config=config.json migratedb` |
| `run` | 启动面板与内部 API，自动拉起自启动的服务器 | `./openvpn-pannel -config=config.json run` |
| `createuser` | 创建用户：`<用户名> <密码> <描述>` | `./openvpn-pannel createuser u1 'P@ssw0rd' 测试用户` |
| `creategroup` | 创建用户组：`<组名>` | `./openvpn-pannel creategroup office` |
| `addusertogroup` | 把用户加入用户组：`<用户名> <组名>` | `./openvpn-pannel addusertogroup u1 office` |
| `democonfig` | 输出示例配置 JSON 到标准输出 | `./openvpn-pannel democonfig > config.json` |
| `version` | 输出构建时间 | `./openvpn-pannel version` |

**批量创建用户示例**

```bash
#!/bin/bash
for i in $(seq 1 50); do
  ./openvpn-pannel -config=config.json createuser "user$i" "Passw0rd_$i" "office-user-$i"
  ./openvpn-pannel -config=config.json addusertogroup "user$i" office
done
```

> 通过 CLI 创建的用户默认限速策略为“依据活跃用户组的最低速率”。如需固定限速/不限速，请在面板中调整。

---

## 4. 配置文件 config.json

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `listen` | string | 面板 Web 监听地址，如 `:8081` 或 `0.0.0.0:8081`。 |
| `mysql_addr` | string | MySQL 地址 `host:port`（使用 MySQL 时必填）。 |
| `mysql_user` | string | MySQL 用户名。 |
| `mysql_pass` | string | MySQL 密码。 |
| `mysql_db` | string | MySQL 数据库名。 |
| `sqlite_db` | string | SQLite 文件路径。**非空时优先使用 SQLite**，忽略 MySQL。 |
| `passwd_salt` | string | 旧版密码哈希（`sha256(密码+盐)`）所用的盐。新密码已改用 **bcrypt**，不再依赖该盐；此项仅用于校验历史哈希，**不要更改**，见第 5.8 节。 |
| `session_secret` | string | 会话签名密钥，建议 32 字符随机串。更改后所有登录会话失效。 |
| `working_dir` | string | 工作目录绝对路径，存放各服务器实例的配置/证书/日志。 |
| `internal_api_listen` | string | 内部 API 监听地址，建议保持 `127.0.0.1:59003`。 |
| `client_page_listen` | string | 面向已连接 VPN 客户端的自助页面监听地址，如 `10.8.0.1:8088`。为空则不启用，见第 5.6 节。 |
| `max_log_size_kb` | int | 单服务器日志容量上限（KB）。达到后自动轮换并清理最早归档；`<=0` 关闭轮换。 |
| `allow_edit_resource` | bool | 是否允许修改/重置脚本等资源。**默认 false**，见第 9、12 节。 |
| `stop_instances_on_exit` | bool | 面板进程退出（SIGINT/SIGTERM）前是否停止所有 OpenVPN 实例。**默认 true**；设为 `false` 时退出/崩溃都不停止实例，面板重启后依据数据库中的 PID 记录重新接管，客户端无感知，见第 5.7 节。 |
| `trusted_proxies` | string[] | 反向代理服务器 IP/CIDR 白名单（如 `["127.0.0.1","10.0.0.0/8"]`）。仅这些来源的 `X-Forwarded-For`/`X-Real-IP` 会被采信为客户端 IP；为空时禁用代理信任。 |


**完整示例**

```json
{
  "listen": ":8080",
  "mysql_addr": "127.0.0.1:3306",
  "mysql_user": "your_mysql_user",
  "mysql_pass": "your_mysql_password",
  "mysql_db": "your_database_name",
  "sqlite_db": "data.db",
  "passwd_salt": "A)$\u0026!^%(Random-String-4312cdab",
  "session_secret": "secret-key-32-byte-long-00001111",
  "working_dir": "/tmp/openvpn-pannel",
  "internal_api_listen": "127.0.0.1:59003",
  "client_page_listen": "0.0.0.0:8088",
  "max_log_size_kb": 16384,
  "allow_edit_resource": false,
  "stop_instances_on_exit": true,
  "trusted_proxies": [
    "127.0.0.1"
  ]
}
```

**只使用 SQLite 的示例**（无需 MySQL，部署最简单）

```json
{
  "listen": ":8080",
  "sqlite_db": "data.db",
  "passwd_salt": "A)$\u0026!^%(Random-String-4312cdab",
  "session_secret": "secret-key-32-byte-long-00001111",
  "working_dir": "/tmp/openvpn-pannel",
  "internal_api_listen": "127.0.0.1:59003",
  "client_page_listen": "0.0.0.0:8088",
  "max_log_size_kb": 16384,
  "allow_edit_resource": false,
  "stop_instances_on_exit": true,
  "trusted_proxies": [
    "127.0.0.1"
  ]
}
```

---

## 5. 面板功能

顶部导航：**用户管理 / 用户组管理 / 服务器管理 / 达量限速 / 证书管理 / 资源管理 / 日志·状态 / 帮助信息**。

### 5.1 用户管理

- 字段：用户名、密码、描述、**限速策略**、流量（总计/当前会话）。
- 支持按用户名搜索、分页、批量删除、清除流量。
- **批量添加**：点“批量添加”，可下载 CSV 模板（表头：`用户名,密码,用户备注,用户组`），填写后上传或粘贴提交。
- **查看**：查看用户详情、所属用户组、在各服务器上的权限与 ACL。
- **更新信息**：修改密码（留空不改）、描述与限速策略；可开启 **TOTP（MFA）** 双因素认证（见第 5.6 节）。
- 角色说明：`admin` 组成员可管理面板；其他用户仅用于 VPN。

限速策略取值：

| 值 | 策略 | 说明 |
| --- | --- | --- |
| 0 | 不设置任何限速策略 | 完全不限速。 |
| 1 | 依据活跃用户组的最低速率（默认） | 在活跃用户组中取**最低**限速。 |
| 2 | 依据活跃用户组的最高速率 | 在活跃用户组中取**最高**限速。 |
| 3 | 固定限速 | 使用该用户自己的上传/下载限速值。 |

### 5.2 用户组管理

- 字段：组名（唯一）、描述、**上传限速**、**下载限速**（单位 KB/s，0=不限速）。
- 一个用户可属于多个组，一个组可包含多个用户。
- **管理** 面板分四个标签页：
  - 管理用户：添加/移除组内用户（支持批量添加、**批量删除**、搜索）；组内用户列表支持分页与每页数量选择。
  - 管理 ACL：增删该组允许访问的网段。
  - 修改信息：修改描述与组限速。
  - 删除用户组：不可逆操作。

### 5.3 服务器管理

创建服务器时需要填写以下信息：

| 字段 | 说明 | 示例 |
| --- | --- | --- |
| 服务器名称 | 唯一名称 | `office-vpn` |
| 监听端口 | OpenVPN 端口 | `1194` |
| 网卡名称 | `dev`，建议写成实际设备名 `tun0` | `tun0` |
| 协议 | `udp` 或 `tcp` | `udp` |
| 拓扑 | 一般为 `subnet` | `subnet` |
| 服务器网段 | 网络地址 + 掩码 | `10.8.0.0 255.255.255.0` |
| 服务器 IPv6 网段 | 对应配置里的 `server-ipv6`，留空表示不启用 IPv6 | `fc00:2048:1024::/64` |
| 心跳配置 | `keepalive` | `10 60` |
| 数据加密算法 | `data-ciphers`，多选/手动输入，以 `:` 分隔 | `AES-256-GCM:AES-128-GCM:CHACHA20-POLY1305` |
| 自动启动 | 面板启动时自动拉起该实例 | 开 |
| 同证书多次登录 | `duplicate-cn` | 关 |
| CA 证书 / 服务器证书 / 私钥 | 可直接粘贴 PEM，或从证书管理选择引用（见第 10 节） | 点击 选择证书 |
| DH 参数 | 见第 10 节，可一键生成（RFC 3526 标准 MODP 组） | 点“一键生成” |
| TLS-Auth | 见第 10 节，可用 openvpn 一键生成 | 点“用 openvpn 生成 ta.key” |
| 其他配置 | 追加到 `server.conf` 的原始配置行 | `push "redirect-gateway def1"` |

- 常用其他配置

|配置内容|说明|
|---|---|
|`mssfix 1308 `| TCP MSS钳制 |
|`push "route 10.0.2.0 255.255.255.0" ` | 推送ipv4路由 |
|`push "route-ipv6 fc00:131::/64"     ` | 推送ipv6路由 |
|`push "dhcp-option DNS [IP 網址]"    ` | 推送DNS地址  |
|`push "redirect-gateway def1"        ` | 推送默认网关 |


- **客户端配置（CCD）**：按“客户端证书名称”下发；实例运行中修改会实时写入 `ccd` 目录。
- **权限**：分“用户权限”和“用户组权限”，各有允许/拒绝，详见第 6 节。
- **启停**：启动会写配置并拉起进程；停止会执行退出脚本并清理该实例的运行时 ACL 记录。
- **进程保活**：面板每 60 秒检查一次已启动实例的 openvpn 进程是否存活，若进程异常退出（崩溃、被 `kill` 等）会自动重新拉起，避免失联；主动“停止”的实例不会被自动拉起。
- **导出客户端配置**：选择服务器与客户端证书、填写地址端口，即可下载 `.ovpn`（见第 10 节）。
- 删除服务器会停止检查、删除数据库记录并清理 `working_dir/<ID>/`。


### 5.4 资源管理

管理内置脚本与模板（见第 9 节）。**默认不可编辑**；如需在线编辑，必须在 `config.json` 中设置
`"allow_edit_resource": true`，否则“编辑/重置”会被禁用，接口返回 403。

### 5.5 日志·状态

- **状态**：服务器版本、管理接口版本、在线客户端列表（用户名、证书名、真实 IP、虚拟 IP、上/下行字节、
  连接时间、被应用的 ACL），并支持**断开**指定客户端。
- **服务器日志**：`openvpn.log`，可按行范围读取、清空。
- **脚本日志**：`auth.log`，记录认证与上下线脚本输出。
- **事件**：按服务器查询/清空审计事件（见第 11 节事件类型）。

### 5.6 客户端自助页面与 MFA（TOTP）

面向**已连接的 VPN 客户端**的只读信息页，监听一个额外端口（`config.json` 的
`client_page_listen`，如 `10.8.0.1:8088`，为空则不启用）。页面通过 HTTP 来源 IP（即客户端
VPN 虚拟 IP）识别客户端，**非 VPN 客户端访问会返回错误**。页面可查看：服务器名称、用户名称、
证书名称、公网 IP、内网 IP、可访问的网络（ACL）、已用流量、当前生效限速。

**为用户开启 MFA**：在“用户管理 → 编辑”中打开“启用 TOTP（MFA）双因素认证”，保存后弹框显示
**二维码**、密钥与 `otpauth://` 链接，用认证器 App（Google/Microsoft Authenticator 等）扫码或手动录入即可。
也可勾选“重新生成密钥”换绑。列表新增 **MFA** 列显示启用状态。

启用 TOTP 后的登录流程：

1. VPN 认证（用户名/密码）通过，但**不立即下发任何 ACL**（网络默认不通）。
2. 用户访问客户端自助页面，页面提示“需要动态验证码”，输入认证器 App 中 30 秒刷新一次的 6 位验证码。
3. 验证通过后，面板调用 `acl_add.sh` 放行该用户应得的 ACL，并展示连接信息；此时出现“登出”按钮。
4. 点击登出（或下线）后，面板调用 `acl_del.sh` **先回收已放行的 ACL，再清空该客户端的
   连接跟踪（状态表）**，使登出前已建立的连接立即失效（需系统安装 `conntrack`，缺失时自动跳过）。
   先删 ACL 再清状态表可避免“清完状态表后、ACL 尚未删除”期间新建的连接被漏放行。

无头设备可用 curl：

```bash
curl http://10.8.0.1:8088/login/123456   # 提交 6 位动态验证码
curl http://10.8.0.1:8088/logout         # 登出并回收 ACL
```

面板登录本身也受 TOTP 保护：启用了 TOTP 的账号在登录页输入用户名密码后，会弹框要求补充
动态验证码，验证通过才进入面板。

> 动态放行 / 回收 ACL 由资源 `acl_add.sh`、`acl_del.sh` 完成，见第 9 节。

---

### 5.7 面板重启时保留 OpenVPN 实例（客户端无感知）

默认情况下，面板收到退出信号（SIGINT/SIGTERM）会先停止所有 OpenVPN 实例再退出。若面板自身
崩溃后被 systemd 等托管进程自动拉起，这期间所有客户端都会断线。

把 `config.json` 的 `stop_instances_on_exit` 设为 `false` 可避免这种中断：

1. 面板**启动实例成功后**，把该实例的 PID 写入数据库（表 `server_process_records`）；
   **停止实例成功后**删除对应记录。
2. 面板退出（含崩溃）时**不停止**任何实例，OpenVPN 子进程成为孤儿进程，由 init/systemd 接管继续运行。
3. 面板（重新）启动时，读取数据库中的 PID 记录：
   - 进程仍存活 → **重新接管**（不重写配置、不重启进程），客户端连接不受影响；
   - 进程已不存在 → 删除记录，若该服务器配置了自启动则按正常流程拉起。

> 注意：要让进程在面板崩溃后被 systemd 重启时存活，托管单元的 `KillMode` 必须为 `process`
> （而不是默认的 `control-group`），否则 systemd 重启服务时会连带杀掉同一 cgroup 内的 OpenVPN 进程。
>
> 该选项仅在面板**能访问同一份数据库**、且 `working_dir` 不变时有效。

---

### 5.8 密码存储、可信代理与登录限流

**密码哈希**：用户密码使用 **bcrypt**（先对密码做一次 SHA-256 再 bcrypt，以支持超过 72 字节的长密码）。
旧版本用 `sha256(密码+passwd_salt)` 存储的历史哈希仍可正常登录，且会在**首次成功登录时自动升级为 bcrypt**，
无需手动重置密码。`passwd_salt` 因此只对历史哈希有意义，不要更改。

**可信代理**：默认不信任任何代理，客户端 IP 一律取 TCP 来源地址，防止伪造 `X-Forwarded-For`。
若面板部署在 Nginx/HAProxy 等反向代理之后，需要在 `config.json` 配置 `trusted_proxies`
（代理服务器的 IP 或 CIDR 列表，如 `["127.0.0.1","10.0.0.0/8"]`），此时来自这些地址的请求才会采用
`X-Forwarded-For`/`X-Real-IP` 作为真实客户端 IP。

**登录限流**：为防止密码与动态验证码被快速暴力枚举，同一用户的**登录密码尝试**与**MFA 验证码尝试**
各自有 **2 秒冷却**：2 秒内的重复尝试会收到 HTTP 429（`retry_after` 为剩余秒数）。两类尝试使用独立的
冷却计时，因此“输入密码 → 弹出验证码框 → 提交验证码”的正常两步登录不会被误伤。

---

### 5.9 IPv6 支持

在“服务器管理”中为服务器填写 **服务器 IPv6 网段**（对应 OpenVPN 的 `server-ipv6`，如
`fc00:2048:1024::/64`）即可为该实例启用 IPv6，留空则不启用。

**是否开启的建议**：即使没有任何 IPv6 业务，也建议开启——否则接口上没有 IPv6 地址，
客户端会认为本机没有 IPv6 网络，可能导致其正常的 IPv6 出公网流量异常。唯一的缺点是极少数
完全禁用了 IPv6 的主机，因无法为接口配置 IPv6 地址而连接失败，此时才需要关闭（留空）。

启用后：

- **地址分配**：客户端除 IPv4 外还会从该网段获得一个 IPv6 虚拟地址（脚本读取 OpenVPN 的
  `ifconfig_pool_remote_ip6` 环境变量）。**虚拟地址在会话期间不变**，因此识别与限速都以虚拟地址为准。
- **ACL**：用户组/用户的 ACL 支持 `4#`(IPv4) 与 `6#`(IPv6 CIDR) 两类。iptables 版脚本分别通过
  `iptables` 与 `ip6tables`（配合 `ipset family inet6`）下发与回收；nftables 版脚本在 `inet` 表内
  分别用 `ipv4_addr` / `ipv6_addr` 集合处理，无需额外维护 ip6tables。
- **带宽限速**：`tc` 同时对 IPv4 与 IPv6 生效。双栈客户端两种地址共用同一个 classid；
  只有 IPv6 的客户端使用由 IPv6 地址推导的独立 classid。
- **流量统计与客户端自助页面**：IPv6 会话同样计入流量统计，自助页面会额外显示“内网 IPv6”。
- 二进制内置脚本、`nftables-scripts/generic/`、`nftables-scripts/openwrt/`、`doc-openwrt/`
  四套脚本均已支持 IPv6。iptables 版依赖系统存在 `ip6tables`；若缺失，IPv6 ACL 会被跳过
  并记录日志（IPv4 不受影响）。

---

## 6. 权限模型

服务器权限由“用户权限”和“用户组权限”共同决定，每条权限为 **允许(PERMIT)** 或 **拒绝(DENY)**。

**优先级**：`用户拒绝 > 用户允许 > 组拒绝 > 组允许`。

| 场景 | 结果 |
| --- | --- |
| 用户被 DENY | 直接拒绝，忽略其它规则。 |
| 用户被 PERMIT | 允许登录。 |
| 用户无用户级规则，但某所属组被 DENY | 拒绝（组拒绝优先于组允许）。 |
| 用户无用户级规则，某所属组被 PERMIT | 允许登录。 |
| 完全没有匹配规则 | **默认拒绝**。 |

### 登录后能得到哪些 ACL

| 通过规则 | 得到的 ACL |
| --- | --- |
| 用户级 PERMIT | 用户**所有**所属组的 ACL（不再看组的权限规则）。 |
| 组级 PERMIT | `组允许的组` ∩ `用户所属的组` 的 ACL；被 DENY 的组除外。 |
| 用户级 DENY | 无。 |
| 无任何权限 / 无匹配组 | 能登录但**不添加任何 ACL**，无法访问受控网络。 |

**示例一：组级授权（取交集）**

- 用户 `u1` 属于：组 A、组 B、组 C
- 服务器允许：组 A、组 C
- `u1` 得到：组 A、组 C 的 ACL；得不到组 B 的 ACL。

**示例二：用户级授权（取并集）**

- 用户 `u2` 属于：组 A、组 B、组 C
- 服务器允许：组 A、组 C（组规则）
- 但给 `u2` 单独设置了**用户级允许**
- `u2` 得到：组 A、组 B、组 C 的 ACL（用户级允许不看组规则）。

**示例三：只给用户权限、无任何组**

- 用户 `u3` 只有用户级允许，不属于任何组
- 结果：可以登录服务器，但**没有任何 ACL**，访问不了任何受控网络。
- 建议：至少给用户加一个组，并在组里配置 ACL。

---

## 7. ACL 与防火墙

### 7.1 ACL 规则

- 类型：`4`（IPv4）或 `6`（IPv6）。
- 值：CIDR，例如 `10.0.0.0/24`、`192.168.1.0/24`、`fc00::/64`。
- 面板会自动校验并**清零主机位**：输入 `10.0.0.5/24` 会保存为 `10.0.0.0/24`。

### 7.2 防火墙联动

客户端上线时，面板根据其权限计算 ACL，并通过脚本写入系统防火墙；下线时回收。

- 若安装了 `ipset`：采用**聚合模式**，性能更好。
  - 相同 ACL 的客户端共享一组对象：源集合 `hash:ip`、目标集合 `hash:net`、子链。
  - 规则条数只与“不同的 ACL 组数”有关，与在线人数无关。
  - 特例：ACL 中包含 `0.0.0.0/0`（全部放行）时，退化为在主链直接 `ACCEPT`（因为 `hash:net` 不接受零前缀网段）。
  - 上下线通过 `flock` 串行化，避免并发改乱防火墙状态。
- 若未安装 `ipset`：回落到**传统模式**，每条 ACL 生成一条 `iptables` 规则。
- 建议安装 `ipset` 与内核模块 `ip_set` / `hash_ip` / `hash_net`。

> 默认脚本仅对 **IPv4 ACL** 生效；IPv6 ACL 会被保存，但默认脚本不应用（可按需自行扩展脚本）。

### 7.3 基础防火墙行为（启动脚本）

服务器启动时会为该实例建立独立链 `openvpn_acl_<服务器ID>`：

1. 放行 `ESTABLISHED,RELATED`；
2. 进入该实例的转发流量先经过链 `openvpn_acl_<ID>`，未匹配则 `DROP`；
3. 出该实例的流量 `ACCEPT`；
4. 对 IPv6 同样建立链（仅放行已建立连接，默认 ACL 未启用）。

---

## 8. 带宽限速（tc）

客户端上线时，面板计算其生效限速并下发 `tc` 规则；下线时回收。

- **单位**：KB/s（千字节每秒，1 KB = 1000 字节）。内部换算：`kbit/s = KB/s × 8`。
- **上传**：服务器 → 客户端，`tc` 出方向（HTB，按目的 IP 整形）。
- **下载**：客户端 → 服务器，`tc` 入方向（police，按源 IP 限速）。
- **0 = 不限速**。

### 8.1 用户限速策略

先看用户策略：

| 值 | 策略 | 计算方式 |
| --- | --- | --- |
| 0 | 不设置任何限速策略 | 上行=0、下行=0（不限速）。 |
| 1 | 依据活跃用户组的最低速率（默认） | 对**活跃用户组**的上/下行限速分别取最低，0 视为“无限大”被忽略。 |
| 2 | 依据活跃用户组的最高速率 | 对活跃用户组的上/下行限速分别取最高；**任一活跃组为 0 则整体不限速**。 |
| 3 | 固定限速 | 使用用户自身的上传/下载限速。 |

### 8.2 活跃用户组

**活跃用户组** = 用户所属、且被该服务器允许连接的用户组（判定与 ACL 权限解析一致）：

- 用户级 `DENY` → 没有活跃组；
- 用户级 `PERMIT` → 用户所属**全部**组都算活跃；
- 否则 → 存在组级 `PERMIT` 且不存在组级 `DENY` 的组才活跃（组 `DENY` 优先）。

**示例**：用户 A 同属“组A、组B”；服务器只允许“组A”连接；则用户 A 连到该服务器时，
**活跃用户组 = 组A**。

### 8.3 计算示例

设活跃用户组：组A =（上行 100、下行 200），组B =（上行 0、下行 50）。

| 用户策略 | 生效上行 | 生效下行 | 说明 |
| --- | --- | --- | --- |
| 1 活跃组最低 | 100 | 50 | 上行取 min(100, 忽略0)=100；下行取 min(200,50)=50。 |
| 2 活跃组最高 | 0 | 200 | 上行有 0（组B）→ 不限速；下行取 max(200,50)=200。 |
| 3 固定（10/20） | 10 | 20 | 直接使用用户值。 |
| 0 不限速 | 0 | 0 | 不做限制。 |

### 8.4 tc 实现要点

- 出方向（上传）：`tc` 根队列 `htb`，每个客户端一个类，按虚拟 IP 的**目的地址**过滤。
- 入方向（下载）：`tc` `ingress` + `police`，按虚拟 IP 的**源地址**过滤并丢弃超额报文。
- 限速对象的编号由虚拟 IP 末两段推导，稳定且唯一；重复上线会先清理同编号规则再重建，避免残留。
- **用户/用户组限速修改后需要客户端重连**（或服务器重启）才会重新下发；**达量限速**的效果变化由状态采集器自动重设（见 8.6），无需重连。

### 8.5 依赖与权限

- 需要安装 `iproute2`（`tc`）及内核模块：`sch_htb`、`cls_u32`、`act_police`、`sch_ingress`。
- 脚本以 `sudo tc ...` 执行，需要在 `sudoers` 中放行 `tc`（见第 12 节）。
- 接口名使用服务器配置的“网卡名称”（`dev`）；请填实际存在的设备名（如 `tun0`），否则跳过限速并记录日志。

### 8.6 达量限速（按流量周期）

“达量限速”用于防止用户滥用：按周期统计用户流量，流量越多限速越严，超额可禁止连接。

- 一个用户最多关联一个限速方案，也可以不关联。
- 方案以一个**周期**为单位（可填分钟/小时/天）；周期一到，自动重置该用户本周期流量统计。
- 方案包含一条或多条**限速规则**，每条规则包含：**优先级**（越大越先匹配）、**上传流量大于**阈值、
  **下载流量大于**阈值、上传/下载限速(KB/s)、是否允许连接。
- 触发方式：**上传或下载任一方向**的周期流量**大于**其阈值，该规则即触发。
- 匹配方式：规则按**优先级从大到小**排列，取**第一条已触发**的规则；所有规则都未触发时不做达量限制（默认档）。
  因此若要“流量越多限制越严”，应让更严格的规则拥有更大的优先级。
- 规则若不允许连接，认证会**直接失败**并提示“请在多久后可继续连接”；若该用户当前在线，会被立即踢下线。
- **生效限速**：先匹配达量限速方案，再与用户限速/活跃用户组限速比较，**取最低值**（0 视为不限速，取最低时忽略）。
- 运行时由状态采集器（每 60 秒）重新计算生效限速，发现变化时调用 `ratelimit.sh`（见第 9 节）
  仅对变化的客户端重新设置 `tc`，无需客户端重连。

“达量限速”页面分两个标签页：**限速方案**（创建/编辑/删除方案、添加/移除关联用户）与**用户状态**
（用户名、限速方案、当前规则优先级、限制；并可一键**重置**该用户本周期流量）。

---

## 9. 资源与脚本

“资源管理”维护以下内置资源。它们保存于数据库；未覆盖时使用程序内置默认值。

| 资源 ID | 说明 | 落地文件名（由“其他配置”决定） |
| --- | --- | --- |
| `config` | 服务器配置模板（`server.conf`） | `server.conf` |
| `client-config` | 客户端配置导出模板（`.ovpn` 渲染用） | — |
| `auth.sh` | 客户端认证脚本 | `_auth.sh` |
| `client_online.sh` | 客户端上线脚本（ACL + 限速） | `_client_online.sh` |
| `client_offline.sh` | 客户端下线脚本（回收 ACL + 限速） | `_client_offline.sh` |
| `ratelimit.sh` | 达量限速运行时更新脚本（仅对限速变化的客户端重设 tc） | —（面板直接执行） |
| `acl_add.sh` | TOTP 验证通过后动态放行用户 ACL（仅启用 MFA 的用户） | —（面板直接执行） |
| `acl_del.sh` | 登出/下线后动态回收用户 ACL | —（面板直接执行） |
| `server_start.sh` | 服务器实例启动脚本（建链） | —（直接执行） |
| `server_exit.sh` | 服务器实例停止脚本（拆链清理） | —（直接执行） |
| `misc` | 其他配置（JSON，见下） | — |
| `help` | 本帮助文档 | — |

### 9.1 安全开关

脚本资源会被以较高权限执行，改写它们等同“任意命令执行”。因此：

- `allow_edit_resource` 默认 `false`，此时“资源管理”的编辑/重置按钮不可用，接口返回 403；
- 确需在线编辑时，在 `config.json` 设置 `"allow_edit_resource": true` 并重启面板；
- 编辑完成后建议改回 `false`。

### 9.2 脚本模板变量

脚本写入前会替换以下占位符：

| 占位符 | 含义 |
| --- | --- |
| `__INTERNAL_API__` | 内部 API 地址（`internal_api_listen`）。 |
| `__WORKING_DIR__` | 该实例的工作目录（末尾带 `/`）。 |
| `__SERVER_ID__` | 服务器实例 ID。 |
| `__SERVER_INTERFACE__` | 服务器网卡名称（`dev`）。 |

### 9.3 OpenVPN 传入的环境变量（常用）

| 场景 | 变量 |
| --- | --- |
| 认证 `auth.sh` | `username`、`password`、`common_name`、`untrusted_ip` / `untrusted_ip6`、`untrusted_port` |
| 上线 `client_online.sh` | `common_name`、`username`、`ifconfig_pool_remote_ip`、`trusted_ip` / `trusted_ip6`、`trusted_port` |
| 下线 `client_offline.sh` | 上述之外还有 `bytes_received`、`bytes_sent`、`time_duration` |

> 说明：OpenVPN 在 IPv4 端点设置 `untrusted_ip`（认证）/ `trusted_ip`（上、下线），在 IPv6 端点改设对应的 `*_ip6` 变量，两者互斥。
> 脚本通过 `REAL_CLIENT_ADDR` 统一取值：IPv4 为 `ip:port`，IPv6 为 `[ip]:port`。

### 9.4 “其他配置” misc 示例

```json
{
  "server_log": "openvpn.log",
  "script_log": "auth.log",
  "openvpn_path": "/sbin/openvpn",
  "management_socket": "management.sock",
  "ca_file_name": "ca.crt",
  "server_cert_file_name": "server.crt",
  "server_key_file_name": "server.key",
  "dh_file_name": "dh2048.pem",
  "ta_file_name": "ta.key",
  "ccd_dir": "ccd",
  "ipp_file_name": "ipp.txt",
  "status_file_name": "openvpn-status.log",
  "server_config_file_name": "server.conf",
  "client_online_script_name": "_client_online.sh",
  "client_offline_script_name": "_client_offline.sh",
  "client_auth_script_name": "_auth.sh",
  "shell_path": "/bin/bash"
}
```

### 9.5 服务器配置模板示例（`config`）

```conf
{{if .ServerConfig.Local}}local {{.ServerConfig.Local}}{{else}};local 0.0.0.0{{end}}
port {{.ServerConfig.Port}}
proto {{.ServerConfig.Proto}}
dev {{.ServerConfig.Dev}}
ca {{.WorkingDir}}{{.MiscConfig.CAFileName}}
cert {{.WorkingDir}}{{.MiscConfig.ServerCertFileName}}
key {{.WorkingDir}}{{.MiscConfig.ServerKeyFileName}}
dh {{.WorkingDir}}{{.MiscConfig.DHFileName}}
data-ciphers {{.ServerConfig.DataCipher}}
server {{.ServerConfig.ServerCIDR}}
topology {{.ServerConfig.Topology}}
management {{.WorkingDir}}{{.MiscConfig.ManagementSocket}} unix
keepalive {{.ServerConfig.Keepalive}}
tls-auth {{.WorkingDir}}{{.MiscConfig.TAFileName}} 0
script-security 3
auth-user-pass-verify {{.WorkingDir}}{{.MiscConfig.ClientAuthScriptName}} via-env
persist-key
persist-tun
status {{.WorkingDir}}{{.MiscConfig.StatusFileName}}
log-append {{.WorkingDir}}{{.MiscConfig.ServerLog}}
verb 3
client-config-dir {{.WorkingDir}}{{.MiscConfig.CCDDir}}
client-disconnect {{.WorkingDir}}{{.MiscConfig.ClientOfflineScriptName}}
client-connect {{.WorkingDir}}{{.MiscConfig.ClientOnlineScriptName}}
{{.ServerConfig.OtherConfig}}
```

> 模板使用 Go 模板语法，可在“资源管理”中按需调整；**不懂不要改**，改错会导致实例无法启动。

---

## 10. 证书与客户端配置

面板内置证书管理：用 Go 标准库生成 CA 与服务器/客户端证书（**不依赖 openssl**，适合 OpenWrt 等环境），
支持导入已有证书，并可一键导出客户端配置。若你更习惯传统方式，也可用 OpenSSL 手动生成后粘贴（见 10.5）。

### 10.1 内置证书管理

入口：顶部导航「证书管理」。

- **生成 CA**：填写名称与证书主题（Common Name、组织 O、组织单位 OU、国家 C、省/州 ST、城市 L、邮箱 emailAddress）、有效期，并选择密钥类型（RSA/ECDSA），生成自签名 CA。
- **导入 CA**：粘贴 CA 证书，私钥可选。**没有私钥的 CA 无法签发新证书**。
- **管理证书**（进入二级页面，针对某个 CA）：
  - 签发证书：点「签发证书」，在弹窗中用单选项选择证书类型——**服务器证书** / **客户端证书** / **未指定**
    （同样可填写上述主题字段、选择密钥类型与有效期）；
  - 导入证书：**必须由该 CA 签发**，否则拒绝；**可以只导入证书、不带私钥**；证书类型**自动识别**；
  - 查看详情、下载证书/私钥、删除。
- **证书类型与扩展密钥用法（EKU）**：证书类型按 OpenVPN `--remote-cert-tls client|server` 使用的
  RFC 3280 规则区分——仅含 serverAuth 用途为**服务器证书**，仅含 clientAuth 用途为**客户端证书**，
  两者都不含或都含则识别为**未指定**。签发时：服务器证书仅写入 serverAuth，客户端证书仅写入 clientAuth，
  未指定则不写入任何扩展密钥用法。导入时同样按上述规则自动识别类型。
- 列表仅返回简要信息（不含证书/私钥本体）；详情弹窗展示主题、颁发者、序列号、生效/失效时间、
  哈希值、密钥与备注，下载文件名形如 `名称-CN-哈希值.cert` / `.key`。
- 密钥类型：RSA（2048/3072/4096）或 ECDSA（P-256/P-384/P-521）。
- 删除限制：CA 下仍有子证书时不能删除；被服务器引用的证书不能删除。
- **证书操作事件**：生成/导入/签发/删除/下载/引用等操作都会记入「证书管理 → 操作事件」，
  事件包含操作人（执行该操作的面板用户）、对端地址、证书与服务器等信息。
  **清空事件后会自动追加一条「清空事件」记录**，标明是谁清理过事件。

### 10.2 在服务器中引用证书

在「服务器管理 → 创建/编辑服务器」中，**CA 证书**、**服务器证书**、**服务器证书私钥** 三个字段：

- 可直接粘贴 PEM 内容；或
- 点「从证书管理选择」，从列表中挑选（服务器证书可选用**服务器证书或未指定**类型），面板会把引用解析为实际 PEM。

**DH 参数**：点“一键生成”，可选 2048/3072/4096 位。使用 RFC 3526 标准 MODP 组（等价于标准
`openssl dhparam` 的固定群），瞬间完成，不需要长时间计算；已有值时会提示将覆盖。

**TLS-Auth**：点“用 openvpn 生成 ta.key”，面板调用 `openvpn --genkey secret`（兼容旧版
`--genkey --secret`）生成。

### 10.3 客户端配置导出

在「服务器管理」点「导出客户端配置」：

1. 选择服务器（会自动带出该实例上次保存的地址与端口）；
2. 选择客户端证书（没有私钥的证书也能导出，只是不含 `<key>` 块）；
3. 填写服务器地址与端口，点“导出”，浏览器即下载 `.ovpn`。

导出时按服务器 CA 的来源分两种模式：

- 服务器 CA 使用“证书管理”选择：客户端证书下拉列出**该 CA 签发的客户端证书与未指定类型证书**；
- 服务器 CA 为手动填写：改为**手动输入客户端证书与私钥**，两者均可留空（导出后自行补全配置文件）。

此外，导出窗口提供「**客户端附加配置**」多行输入（如 `mssfix 1308`、`redirect-gateway def1`），
内容会**追加到导出配置末尾**，并与服务器地址、端口一起**按服务器实例保存**，下次导出自动回填。
可在其中添加 `remote-cert-tls server` 要求服务端证书含 serverAuth 用途；在服务器的「其他配置」中
添加 `remote-cert-tls client` 可要求客户端证书含 clientAuth 用途（面板默认不写入，以保证旧证书兼容）。

地址与端口**按服务器实例保存**，下次导出自动回填。导出内容由资源 `client-config` 渲染，
使用内联 `<ca>/<cert>/<key>/<tls-auth>`，单文件即可导入使用；需要自定义时可在“资源管理”中修改该模板。

### 10.4 客户端 .ovpn 参考模板

```conf
client
dev tun
proto udp
remote <服务器IP> <端口>
resolv-retry infinite
nobind
persist-key
persist-tun
auth-user-pass
verb 3

mssfix 1308

<ca>
-----BEGIN CERTIFICATE-----
（CA 证书内容）
-----END CERTIFICATE-----
</ca>
<cert>
-----BEGIN CERTIFICATE-----
（客户端证书内容）
-----END CERTIFICATE-----
</cert>
<key>
-----BEGIN PRIVATE KEY-----
（客户端私钥内容）
-----END PRIVATE KEY-----
</key>
<tls-auth>
-----BEGIN OpenVPN Static key V1-----
（ta.key 内容）
-----END OpenVPN Static key V1-----
</tls-auth>
key-direction 1
```

- mssfix 字段说明  
在某些网络下，特别是移动网络，mtu没有1500，例如只有1400，在当前协议配置和udp ipv4模式下，此时需要设置mssfix为mtu-92也就是1300。  

- 计算方式  
简单来说，**tcp mss为物理网络mtu-92**。   
当前协议配置和udp ipv4隧道下，物理网络mtu 1400，虚拟tun接口mtu 1348，ipv4报文和tcp头部各占用20字节，所以tcp mss为1348-20-20=1308。  
- 是否需要设置？  
推荐为客户端设置不同的mssfix，也可以在服务器端统一设置一个比较小的mssfix。  
不设置会增加tcp握手时间，体验上就是打开网页缓慢。

### 10.5 用 OpenSSL 手动生成（可选）

如果你不想使用面板内置的证书管理，也可以用 OpenSSL/OpenVPN 命令手动生成，再把内容粘贴到服务器字段：

```bash
# --- DH 参数 ---
openssl dhparam -out ./dh2048.pem 2048

# --- TLS-Auth 密钥 ---
openvpn --genkey tls-auth ./ta.key

# --- CA 私钥与证书 ---
openssl genrsa -out ./ca.key 2048
openssl req -new -sha256 -key ./ca.key -out ./ca.csr -subj "/C=CN/CN=server-ca"
openssl x509 -req -days 36500 -sha256 -signkey ./ca.key -in ./ca.csr -out ./ca.crt

# --- 服务器私钥与证书 ---
openssl genrsa -out ./server.key 2048
openssl req -new -sha256 -key ./server.key -out ./server.csr \
  -subj "/C=CN/ST=JiangSu/L=NanJing/CN=server-cert/emailAddress=server@example.com"
openssl x509 -req -days 36500 -sha256 -CA ./ca.crt -CAkey ./ca.key -CAcreateserial \
  -in ./server.csr -out ./server.crt

# --- 客户端私钥与证书（可重复生成多个） ---
openssl genrsa -out ./client1.key 2048
openssl req -new -sha256 -key ./client1.key -out ./client1.csr \
  -subj "/C=CN/ST=Shanghai/L=PuDong/CN=client1-cert/emailAddress=client1@example.com"
openssl x509 -req -days 36500 -sha256 -CA ./ca.crt -CAkey ./ca.key -CAcreateserial \
  -in ./client1.csr -out ./client1.crt
```

把 `ca.crt`、`server.crt`、`server.key`、`dh2048.pem`、`ta.key` 的内容分别填入服务器设置的对应字段，
即可与内置证书管理等价使用。

### 10.6 客户端专属配置（CCD）示例

在“服务器管理 → 客户端配置”中，按**客户端证书名称**添加：

```conf
# 指定客户端固定虚拟 IP
ifconfig-push 10.8.0.100 255.255.255.0

# 指定通往该客户端内网的路由（服务器端生效）
iroute 192.168.17.0 255.255.255.0

# 向该客户端推送路由
push "route 192.168.1.0 255.255.255.0"
```

---

## 11. 日志、状态与轮换

### 11.1 日志文件

| 日志 | 默认文件 | 说明 |
| --- | --- | --- |
| 服务器日志 | `openvpn.log` | OpenVPN 运行日志，可在“日志·状态”查看/清空。 |
| 脚本日志 | `auth.log` | 认证与上下线脚本输出（含 ACL/限速设置结果）。 |

### 11.2 日志轮换

当某实例的日志（含其归档）总量超过 `max_log_size_kb` 时：

1. 以 `copytruncate` 方式归档当前日志，文件名带纳秒时间戳；
2. 从最早的归档开始删除，直到总量回落到上限的 **80%**；
3. 始终**至少保留最新一个归档**。

`max_log_size_kb <= 0` 时关闭轮换。

### 11.3 事件类型

| 值 | 事件 |
| --- | --- |
| 1 | 客户端上线 |
| 2 | 客户端下线 |
| 3 | 客户端认证失败 |
| 4 | 客户端认证成功 |
| 5 | 服务器启动成功 |
| 6 | 服务器退出成功 |
| 7 | 服务器启动失败 |
| 8 | 服务器退出失败 |
| 9 | 添加 ACL |
| 10 | 删除 ACL |
| 11 | 一次认证成功（启用二次认证的客户端，用户名密码通过） |
| 12 | 二次认证成功（启用二次认证的客户端，TOTP 通过并放行 ACL） |
| 13 | 二次认证下线（在客户端自助页面登出；若未登出则随 VPN 会话结束时自动补记） |
| 14 | 一次认证下线（启用二次认证的客户端，VPN 会话结束） |
| 15 | 服务器已恢复（异常退出后被看门狗自动拉起） |

---

## 12. 安全建议

1. **资源编辑开关**：保持 `allow_edit_resource = false`；仅在需要时临时开启。
2. **密钥强度**：`passwd_salt`、`session_secret` 使用足够长的随机字符串；不要随意外泄或更改。
3. **内部 API**：`internal_api_listen` 绑定 `127.0.0.1`，不要暴露到公网。
4. **最小权限 sudoers**：非 root 运行程序时，仅放行必要命令：

   ```sudoers
   # 请按实际路径和实际用户名（username）替换
   username ALL=(ALL) NOPASSWD: /usr/sbin/iptables
   username ALL=(ALL) NOPASSWD: /usr/sbin/ip6tables
   username ALL=(ALL) NOPASSWD: /usr/sbin/ipset
   username ALL=(ALL) NOPASSWD: /usr/sbin/tc
   ```

5. **OpenVPN 能力**：以 root 运行可跳过此步；否则给二进制授予网络管理能力：

   ```bash
   sudo setcap cap_net_admin+ep /usr/sbin/openvpn
   ```

6. **面板访问**：建议仅内网访问，或前置 HTTPS 反向代理；`admin` 组不要放 VPN 普通用户。

---

## 13. 部署（systemd）

将 `openvpn-pannel.service` 放在 `/etc/systemd/system/` 或 `/lib/systemd/system/`：

```ini
[Unit]
Description=openvpn-pannel
After=network.target nss-lookup.target

[Service]
User=openvpn
AmbientCapabilities=CAP_NET_ADMIN CAP_NET_BIND_SERVICE
CapabilityBoundingSet=CAP_IPC_LOCK CAP_NET_ADMIN CAP_NET_BIND_SERVICE CAP_NET_RAW CAP_SETGID CAP_SETUID CAP_SYS_CHROOT CAP_DAC_OVERRIDE CAP_AUDIT_WRITE
WorkingDirectory=/opt/openvpn-pannel
ExecStart=/opt/openvpn-pannel/openvpn-pannel -config /opt/openvpn-pannel/config.json run
Restart=on-failure
RestartPreventExitStatus=23
ReadWritePaths=/opt/openvpn-pannel

[Install]
WantedBy=multi-user.target
```

```bash
sudo systemctl daemon-reload
sudo systemctl enable --now openvpn-pannel
```

---

## 14. 常见问题（FAQ）

**Q1：普通用户能登录面板吗？**
不能。只有 `admin` 组成员的用户可登录面板；其他用户仅用于 VPN 认证。

**Q2：用户能认证成功，但访问不了任何网络？**
检查：该用户/其用户组是否在目标服务器上有**允许**权限；对应组是否配置了 ACL。若只有用户权限、没有任何组，
或没有任何匹配的权限规则，则不会下发 ACL。

**Q3：修改了 ACL / 权限 / 限速，为什么不生效？**
ACL 与限速在客户端**上线时**下发。修改后请让客户端**重新连接**；服务器配置（`server.conf` 相关）修改后需
**重启服务器实例**。

**Q4：限速没有生效？**
依次检查：是否安装了 `tc` 及内核模块；`sudoers` 是否放行 `tc`；“网卡名称”是否为实际设备名（如 `tun0`）；
策略计算后是否确实 > 0；客户端是否已重连。

**Q5：IPv6 ACL 不生效？**
默认脚本仅应用 IPv4 ACL；IPv6 ACL暂未实现，IPv6 ACL 会保存但不会自动下发，可按需扩展脚本。

**Q6：资源管理里的按钮是灰色的？**
`allow_edit_resource` 为 `false`。在 `config.json` 设为 `true` 并重启面板即可。

**Q7：忘记了 admin 密码？**
用 CLI 创建/调整：`./openvpn-pannel createuser ...`、`addusertogroup`；或直接在“用户管理”中由其他 admin 重置。

**Q8：用 MySQL 还是 SQLite？**
`sqlite_db` 非空时优先用 SQLite，适合单机、部署简单；多实例/高并发可选 MySQL（`sqlite_db` 置空）。

**Q9：为什么日志目录越来越大？**
设置 `max_log_size_kb`（如 `20480` 表示 20 MB）启用自动轮换；`<=0` 表示关闭。

---

## 15. 附录

### 15.1 工作目录结构

```
working_dir/
└── <服务器ID>/
    ├── server.conf            # 渲染后的 OpenVPN 配置
    ├── ca.crt / server.crt / server.key / dh2048.pem / ta.key
    ├── ccd/                   # 客户端专属配置（按证书名）
    ├── ipp.txt                # 虚拟 IP 持久化
    ├── openvpn.log            # 服务器日志
    ├── openvpn-status.log     # 状态文件
    ├── management.sock        # 管理套接字
    ├── _auth.sh / _client_online.sh / _client_offline.sh
    └── auth.log               # 脚本日志
```

### 15.2 主要数据表

| 表 | 用途 |
| --- | --- |
| `users` | 用户、密码哈希、流量、限速策略 |
| `groups` | 用户组、描述、组限速 |
| `user_groups` | 用户与用户组的多对多关系 |
| `group_acls` | 用户组 ACL（类型 + CIDR） |
| `servers` | 服务器实例配置与证书（CA/证书/私钥字段可为 `cert-stor:` 引用） |
| `certificates` | 证书管理：CA / 服务器 / 客户端证书与私钥、归属 CA、有效期 |
| `server_routes` | 推送路由 |
| `client_configs` | 客户端专属配置（CCD） |
| `server_permissions` | 服务器权限（用户/组，允许/拒绝） |
| `app_resource_records` | 资源（脚本/模板/misc/help） |
| `server_events` | 事件审计 |
| `certificate_events` | 证书操作审计（生成/导入/签发/删除/下载/服务器引用/客户端引用） |
| `rate_limit_plans` | 达量限速方案（周期） |
| `rate_limit_rules` | 达量限速规则（流量阈值、限速、是否允许连接） |
| `connected_client_info_records` | 在线客户端会话流量与已下发的限速 |
| `added_server_acl_records` | 本次会话已下发的 ACL（用于下线回收） |

### 15.3 相关内核模块

- 防火墙：`ip_set`、`ip_set_hash_ip`、`ip_set_hash_net`（使用 `ipset` 时需要）。
- 限速：`sch_htb`、`cls_u32`、`act_police`、`sch_ingress`。

---

> 本帮助文档本身也是“资源管理”中的 `help` 资源。开启 `allow_edit_resource` 后，你可以按自己的环境修改它。
